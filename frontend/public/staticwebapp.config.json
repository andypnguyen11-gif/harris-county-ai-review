{
  "navigationFallback": {
    "rewrite": "/index.html",
    "exclude": ["*.{css,js,ico,png,svg,webp,woff,woff2,txt,json,map}"]
  },
  "globalHeaders": {
    "X-Content-Type-Options": "nosniff",
    "Referrer-Policy": "same-origin"
  }
}
